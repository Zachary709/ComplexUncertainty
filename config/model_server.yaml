# 模型服务器配置文件

# 服务器配置
server:
  host: "0.0.0.0"
  port: 8000

# 模型配置
model:
  path: "/home/zhangdw/models/Qwen/Qwen3-8B"
  torch_dtype: "bfloat16"
  device_map: "auto"
  trust_remote_code: true
  output_hidden_states: true

# 默认生成参数
generation:
  max_new_tokens: 100
  do_sample: false
  temperature: 1.0
  top_p: 1.0
  apply_template: true
  system_prompt: "You are a helpful assistant."
  enable_thinking: true
